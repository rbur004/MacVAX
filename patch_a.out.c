#define _PATCH_A_OUT_C_#include 	"a.out.h"#include 	"structs.h"#include 	"vax_to_68000.h"#include 	"patch_a.out.h"#pragma segment SYMBOL_TABLEvoid patch_header(	struct exec_struct *h){	h->a_magic = v2m_long(h->a_magic);	/* magic number */	h->a_text = v2m_long(h->a_text);		/* size of text segment */	h->a_data = v2m_long(h->a_data);		/* size of initialized data */	h->a_bss = v2m_long(h->a_bss);		/* size of uninitialized data */	h->a_syms = v2m_long(h->a_syms);		/* size of symbol table */	h->a_entry = v2m_long(h->a_entry);	/* entry point */	h->a_trsize = v2m_long(h->a_trsize);	/* size of text relocation */	h->a_drsize = v2m_long(h->a_drsize);	/* size of data relocation */}void patch_rel_info(	RELINFO *r){	r->r_address = v2m_long(r->r_address);	r->x.r_composite = v2m_long(r->x.r_composite);}void patch_nlist(	struct nlist *n){	n->n_un.n_strx 	= v2m_long(n->n_un.n_strx);	n->n_desc 		= v2m_short(n->n_desc);	n->n_value		= v2m_long(n->n_value);}